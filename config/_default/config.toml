# Change baseurl before deploy
baseurl = "https://leocha.ng"
# languageCode was deprecated in Hugo v0.158.0 and will be removed in a future release.
# Use `locale` (and/or `defaultContentLanguage` / language config) instead.
# languageCode = "zh-cn"
title = "Leo Chang's site"

# Theme i18n support
# Available values: en, fr, id, ja, ko, pt-br, zh-cn, zh-tw, es, de, nl, it, th, el, uk, ar
locale = "zh-cn"

# Set hasCJKLanguage to true if DefaultContentLanguage is in [zh-cn ja ko]
# This will make .Summary and .WordCount behave correctly for CJK languages.
hasCJKLanguage = false

# Change it to your Disqus shortname before using
disqusShortname = "leochang"

[pagination]
pagerSize = 5
